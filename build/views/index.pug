extends layout
block content
    .jumbotron 
        .container
            h1 Siaradwch mwy Cymraeg!
            p Find Welsh practice groups in your local area. Click on one of the buttons below to get started    
            each region in regions
                p
                    a.btn.btn-primary.bln-lg(href=`/regions/${region.id}/index.htm`) #{region.title}
            p About            