extends layout

block content
    h1 #{region.title}
    p TODO - show a preview of upcoming stuff here? Show the calendar?
    ul
        li
            a(href=`regular.htm`) Regular meetings
        li
            a(href=`oneoff.htm`) One off meetings
        li
            a(href=`upcoming.htm`) Calendar
